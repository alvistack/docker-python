#!/bin/bash

# {{ ansible_managed }}

set -o xtrace

# Prepend executable if command starts with an option
if [ "${1:0:1}" = '-' ]; then
    set -- {{ python_command[python_release] }} "$@"
fi

exec "$@"
